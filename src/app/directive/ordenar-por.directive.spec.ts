import { OrdenarPorDirective } from './ordenar-por.directive';

describe('OrdenarPorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdenarPorDirective();
    expect(directive).toBeTruthy();
  });
});
